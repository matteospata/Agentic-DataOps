# Data quality rules

Revenue should be non-null and non-negative. Orders should be a non-negative integer. Customer identifiers should be present and unique when the dataset represents one row per customer. A quality report must identify the affected column, severity, rule name, and observed value.

Agents must describe quality risks before presenting business conclusions. SQL execution is read-only and every tool call must be written to the audit log.

