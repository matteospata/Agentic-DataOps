# Data contracts

Data contracts define the expected structure and semantics of every dataset. A contract should document column names, data types, nullability, uniqueness, accepted ranges, and ownership.

The sales dataset uses `date` as the event date, `region` as the reporting dimension, `revenue` as the monetary metric, `orders` as the transaction count, and `customer_id` as the customer identifier.

